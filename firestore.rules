rules_version = '2';

// Firestore opcional (respaldo). La base de datos principal es Google Sheets.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
