/**
 * DEPRECADO — Firebase Auth eliminado del panel admin.
 * Este archivo no se publica en Hosting (ver firebase.json ignore).
 * Conservado solo como referencia local; usar js/firebase-config.example.js si se reactiva auth.
 */
window.FIREBASE_CONFIG = null;
window.ALLOWED_ADMIN_EMAIL = null;
