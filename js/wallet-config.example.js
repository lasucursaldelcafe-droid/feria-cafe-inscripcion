/**
 * Configuración Google Wallet (opcional).
 * Copia a js/wallet-config.js si necesitas sobreescribir la URL de la Cloud Function.
 */
window.WALLET_CONFIG = {
  // URL completa de generateWalletPass (si difiere del auto-detect por projectId)
  // FUNCTION_URL: 'https://us-central1-la-sucursal-del-cafe.cloudfunctions.net/generateWalletPass',
  ENABLED: true
};
